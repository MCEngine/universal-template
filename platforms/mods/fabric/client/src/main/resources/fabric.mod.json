{
  "schemaVersion": 1,
  "id": "${modId}",
  "version": "${version}",
  "name": "${modName}",
  "description": "Client half of the universal plugin template. Sends actions to the server over a plugin message channel.",
  "authors": ["MCEngine"],
  "license": "MIT",
  "environment": "client",
  "entrypoints": {
    "client": ["${entrypoint}"]
  },
  "depends": {
    "fabricloader": ">=${loader}",
    "fabric-api": "*",
    "minecraft": "~${minecraft}"
  }
}
