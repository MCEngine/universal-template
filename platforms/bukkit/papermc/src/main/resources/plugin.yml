name: ${name}
version: ${version}
main: ${main}
api-version: '${apiVersion}'
folia-supported: ${foliaSupported}
author: MCEngine
description: Universal plugin template
commands:
  ${command}:
    description: The template's example command
    usage: /${command} <ping|greet [text]>
